import { Token, TokenValue, TokenType } from '../tokenizer';

/**
 * Parses tokens into expressions.
 */
export class TokenParser
{
    /**
     * The tokens to be parsed.
     */
    private _tokens: Array<Token>;

    
    constructor(tokens: Array<Token>)
    {

    }
}